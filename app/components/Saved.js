import React, { Component } from "react";

class Saved extends Component {
  addComment(comment) {
  	// API.addComment(article).then(this.props.getSaved);
  }
  render() {
    
  }
}

export default Saved;
